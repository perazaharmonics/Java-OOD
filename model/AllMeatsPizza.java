package model;

public class AllMeatsPizza extends Pizza {
    public AllMeatsPizza(String title, float price, int size, int orderSize) {
        super(title, price, size, orderSize);
        // You can set additional properties specific to AllMeatsPizza here
    }
    
    // Other methods specific to AllMeatsPizza
}