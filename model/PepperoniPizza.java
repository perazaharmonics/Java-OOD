package model;

public class PepperoniPizza extends Pizza {
    public PepperoniPizza(String title, float price, int size, int orderSize) {
        super(title, price, size, orderSize);
       
    }
}
