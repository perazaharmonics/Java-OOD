package model;

public class CheesePizza extends Pizza {
    public CheesePizza(String title, float price, int size, int orderSize) {
        super(title, price, size, orderSize);
        
    }
}