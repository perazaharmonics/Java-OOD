package model;

public class VegetablePizza extends Pizza {
    public VegetablePizza(String title, float price, int size, int orderSize) {
        super(title, price, size, orderSize);
        // You can set additional properties specific to VegetablePizza here
    }
}
